# config.env - Static configuration (in Git; updated by code_update)
# Secrets (DEVICE_ID, API_TOKEN) go in secrets.env - copy from USB after bootstrap

# GitHub (for code_update)
GITHUB_REPO_OWNER=azikatti
GITHUB_REPO_NAME=Berlin-dooh-device
GITHUB_REPO_BRANCH=main

# API (backend base URLs - same for all devices)
API_URL=https://piapi.speakinprivate.com/api/v1/campaign/playlist/
HOST_URL=https://piapi.speakinprivate.com
